import { Modal } from '@fsm/ui';

import { ModalProps } from '../../../../../models';

export default function LocalModal(props: ModalProps) {
  return (
    <Modal {...props}>
      <div>hell</div>
    </Modal>
  );
}
